# 📦 Гайд по встановленню перекладів

## 🎯 Як працює автоматичне встановлення

Little Bit автоматично шукає ігри у вашій системі:

### Windows:
- **Steam**: Читає registry `HKEY_LOCAL_MACHINE\SOFTWARE\Valve\Steam`
- **GOG**: Шукає в `C:\Program Files (x86)\GOG Galaxy\Games`
- **Epic**: Шукає в `C:\Program Files\Epic Games`

### macOS:
- **Steam**: `~/Library/Application Support/Steam`
- **GOG**: `~/Applications/GOG Galaxy`
- **Epic**: `/Applications`

### Linux:
- **Steam**: `~/.steam/steam` або `~/.var/app/com.valvesoftware.Steam/.steam/steam` (Flatpak)

---

## 🚀 Процес встановлення

Коли ви натискаєте "Встановити переклад":

### 1. Детекція гри (5-10%)
Додаток шукає гру на вашому комп'ютері через Steam/GOG/Epic.

**Якщо гра не знайдена:**
- Показується помилка: "Гру не знайдено"
- Можна буде обрати папку вручну (TODO)

### 2. Завантаження (10-70%)
Завантажує переклад з GitHub Releases:
```
https://github.com/LittleBitUA/LittleBitTR/releases/download/[release-tag]/translation.zip
```

### 3. Розпакування (70-80%)
Розпаковує ZIP архів у тимчасову папку.

### 4. Копіювання (80-95%)
Копіює файли перекладу в папку гри.

**Типові шляхи:**
```
Steam: [game-path]/steamapps/common/[Game Name]/
GOG: [game-path]/Games/[Game Name]/
Epic: [game-path]/[Game Name]/
```

### 5. Очищення (95-100%)
Видаляє тимчасові файли.

---

## 📝 Структура архіву перекладу

Архів `translation.zip` повинен містити файли у правильній структурі:

### Приклад для Yakuza Kiwami 2:
```
translation.zip
├── data/
│   ├── fonts/
│   │   └── ukrainian.ttf
│   ├── localization/
│   │   ├── strings_ua.csv
│   │   └── dialogues_ua.txt
│   └── ui/
│       └── textures_ua.pak
└── README.txt
```

**Важливо:** Структура всередині ZIP має відповідати структурі папки гри!

---

## ⚙️ Налаштування games.json

У репозиторії `LittleBitTR` файл `games.json` містить шляхи встановлення:

```json
{
  "id": "yakuza-kiwami-2",
  "installPaths": {
    "steam": "data",           // Відносний шлях від папки гри
    "gog": "data"
  },
  "downloadUrl": "https://github.com/LittleBitUA/LittleBitTR/releases/download/yakuza-k2-v1.0.0/translation.zip"
}
```

### Якщо `installPaths` порожній:
Файли копіюються безпосередньо в кореневу папку гри.

### З `installPaths`:
Файли копіюються в підпапку. Наприклад:
```
Steam: C:/Games/Steam/steamapps/common/Yakuza Kiwami 2/data/
       ↑ gamePath                                    ↑ installPath
```

---

## 🔧 Як підготувати переклад для публікації

### 1. Організуйте файли:
```bash
cd ~/my-translation
mkdir -p yakuza-k2-translation/data
# Покладіть всі файли перекладу в правильну структуру
```

### 2. Створіть архів:
```bash
cd yakuza-k2-translation
zip -r translation.zip ./*
```

**Важливо:** ZIP архів НЕ повинен містити кореневу папку!

**Правильно:**
```
translation.zip
├── data/
│   └── fonts/
└── README.txt
```

**Неправильно:**
```
translation.zip
└── yakuza-k2-translation/    ← НЕ додавати!
    ├── data/
    └── README.txt
```

### 3. Завантажте на GitHub:
```bash
gh release create yakuza-k2-v1.0.0 \
  translation.zip \
  --repo LittleBitUA/LittleBitTR \
  --title "Yakuza Kiwami 2 v1.0.0"
```

### 4. Оновіть games.json:
```bash
# Додайте або оновіть downloadUrl
"downloadUrl": "https://github.com/LittleBitUA/LittleBitTR/releases/download/yakuza-k2-v1.0.0/translation.zip"
```

---

## 🐛 Вирішення проблем

### Проблема: "Гру не знайдено"

**Рішення:**
1. Переконайтесь, що гра встановлена
2. Перевірте, що гра встановлена через Steam/GOG/Epic
3. Спробуйте обрати папку вручну (кнопка "Обрати папку")

### Проблема: "Помилка завантаження"

**Рішення:**
1. Перевірте інтернет-з'єднання
2. Переконайтесь, що реліз існує на GitHub
3. Перевірте URL в `games.json`

### Проблема: "Помилка розпакування"

**Рішення:**
1. Перевірте, що файл `translation.zip` не пошкоджений
2. Спробуйте розпакувати архів вручну
3. Перевірте формат архіву (має бути ZIP)

### Проблема: "Помилка копіювання"

**Рішення:**
1. Закрийте гру перед встановленням
2. Переконайтесь, що у вас є права на запис у папку гри
3. Запустіть Little Bit від імені адміністратора (Windows)

---

## 📊 Приклад повного робочого процесу

### Для розробника перекладу:

```bash
# 1. Підготуйте переклад
cd ~/my-yakuza-translation
mkdir -p data/localization
# ... додайте файли ...

# 2. Створіть архів
zip -r translation.zip ./data

# 3. Завантажте на GitHub
gh release create yakuza-k2-v1.0.0 \
  translation.zip \
  --repo LittleBitUA/LittleBitTR \
  --title "Yakuza Kiwami 2 v1.0.0" \
  --notes "Повний переклад Yakuza Kiwami 2"

# 4. Оновіть games.json
cd ~/LittleBitTR
# Відредагуйте games.json
git add games.json
git commit -m "Release Yakuza Kiwami 2 v1.0.0"
git push
```

### Для користувача:

```bash
# 1. Запустіть Little Bit
pnpm dev

# 2. Оберіть гру зі списку
# 3. Натисніть "Встановити переклад"
# 4. Дочекайтесь завершення (5-30 секунд залежно від розміру)
# 5. Запустіть гру і насолоджуйтесь українською мовою! 🎮
```

---

## 🔒 Безпека

- Всі файли завантажуються через HTTPS
- GitHub Releases підписані та безпечні
- Додаток не змінює системні файли
- Додаток не потребує прав адміністратора (крім Windows у деяких випадках)

---

## 📞 Підтримка

Якщо у вас виникли проблеми:
1. Перевірте логи в консолі (DevTools: Ctrl+Shift+I)
2. Створіть issue на GitHub
3. Напишіть в наш Discord/Telegram

---

## ✅ Checklist для тестування

- [ ] Гра детектується автоматично
- [ ] Завантаження показує правильний прогрес
- [ ] Архів розпаковується без помилок
- [ ] Файли копіюються в правильну папку
- [ ] Тимчасові файли видаляються
- [ ] Переклад працює в грі
- [ ] Можна встановити оновлення

Готово! 🎉
